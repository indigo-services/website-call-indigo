# v0.9.0 Repo Linking Issue Pack

Use this pack to create the GitHub issues in `launchops` once `gh` auth is repaired.

## Parent Issue

Title:

`v0.9.0 prod repo-linking and release hygiene`

Acceptance:

- repo ownership for `studio.call-indigo.com` and `call-indigo.com` is explicit
- release/docs/checklists reflect the real production contract
- salvage work is moved off local `main`
- release notes and tag path are defined for `v0.9.0`

## Child Issues

### 1. Indigo Studio: salvage local deploy and repo tweaks chore into issue-scoped PR

Acceptance:

- existing local `main` work is preserved on a dedicated branch
- local `main` returns to parity with `origin/main`
- PR notes identify local-only noise vs release material

### 2. Indigo Studio: verify EasyPanel production repo linkage and deploy contract

Acceptance:

- EasyPanel points to `indigo-services/indigo-studio`
- compose source is Git-backed
- build path, compose path, and internal service naming are documented and verified
- `studio.call-indigo.com` is the stated production Strapi domain

### 3. Website Call Indigo: verify Vercel production repo linkage for call-indigo.com

Acceptance:

- Vercel production source of truth is verified
- `call-indigo.com` ownership is documented
- any required frontend repo follow-up is tracked in the owning repo

### 4. Indigo Studio: update docs, checklist, and release protocol to official domains

Acceptance:

- deploy docs use `studio.call-indigo.com` and `call-indigo.com`
- docs state the repo boundary between studio and website repos
- release checklist matches the real production contract

### 5. Indigo Studio: document content publish contract for local SQLite vs production

Acceptance:

- docs explicitly state that local SQLite content is not auto-published
- migration or manual publish ownership is required for content-bearing releases

### 6. Indigo Studio: repair repo-local GHP workspace config detection

Acceptance:

- `ghp` resolves `.ghp/config.json` from inside the repo
- `yarn ghp:config:show` no longer reports `Workspace: (not in a git repository)`
- the fallback rule is removed once the CLI behavior is fixed

## Release Notes Template

- scope: repo linking and release hygiene
- owning repos: `indigo-studio`, `website-call-indigo`
- domains: `studio.call-indigo.com`, `call-indigo.com`
- validation proof: EasyPanel deploy, Vercel deploy, domain checks
- rollback note: one line per repo
