# Generated by yarn toolset:init
# Ignored local user/global provider tokens and repo-wide utility config
# Windows user-level env vars are honored by the loader, so secrets can stay global.

# OPENAI_API_KEY (provider=openai, scope=global, secret, optional)
# Optional user-level token for OpenAI-powered tooling and local automation. Can come from Windows user-level env vars or .env.local.
OPENAI_API_KEY=03f0291cec6148de940055549da087d0.lzchj0lsswHqsTud

# ZAI_API_KEY (provider=zai, scope=global, secret, optional)
# Z.AI API key for OpenAI-compatible clients and local tooling. Can come from Windows user-level env vars or .env.local.
ZAI_API_KEY=03f0291cec6148de940055549da087d0.lzchj0lsswHqsTud

# ZAI_OPENAI_BASE_URL (provider=zai, scope=global, plain, optional)
# Z.AI OpenAI-compatible base URL for the general API endpoint. Safe to keep locally because it is not a secret.
ZAI_OPENAI_BASE_URL=https://api.z.ai/api/paas/v4

# GITHUB_TOKEN (provider=github, scope=global, secret, optional)
# Optional user-level token for GitHub API access, repo sync, and CI metadata.
GITHUB_TOKEN=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

# GITHUB_REPOSITORY (provider=github, scope=global, plain, optional)
# Default repository slug used by provider-aware scripts when the repo cannot be inferred.
GITHUB_REPOSITORY=indigo-services/indigo-studio

# VERCEL_TOKEN (provider=vercel, scope=global, secret, optional)
# Optional Vercel token for deployment, environment sync, and runtime inspection.
VERCEL_TOKEN=vck_71sNbPgw8DTBWS77wRdb65XTh3q2WB7zLrnh5MJA2a0zkjiDir1VJvIR

# VERCEL_PROJECT_ID (provider=vercel, scope=global, plain, optional)
# Project identifier for the frontend deployed on Vercel.
VERCEL_PROJECT_ID=indigo-studio

# VERCEL_TEAM_ID (provider=vercel, scope=global, plain, optional)
# Team identifier for Vercel organization-level access.
VERCEL_TEAM_ID=indigo-services
